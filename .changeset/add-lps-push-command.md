---
"@loopress/cli": minor
---

Add `lps push`, a top-level command that pushes plugins, composer dependencies, ACF, API routes, forms, pages, SEO, and snippets to WordPress in one run instead of calling each resource's `push` command separately. Production pushes are confirmed once up front rather than once per resource.
