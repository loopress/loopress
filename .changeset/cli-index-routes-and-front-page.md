---
"@loopress/cli": minor
---

`lps api push` refuses a root `api/index.php` before any network call, with a message pointing to a named or nested file (`orders/index.php` is the `/orders` route). `lps page push` says when a pushed `pages/home.html` became, or stopped being, the site's front page.
