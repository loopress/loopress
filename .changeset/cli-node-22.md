---
"@loopress/cli": minor
---

The CLI now requires Node.js 22 or later (previously declared as 18+, but `lps validate` already relied on Node 20.12+ APIs). Node 22 is the oldest release line still receiving security fixes.
