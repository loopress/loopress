---
"@loopress/cli": minor
---

Add `lps dev`, which watches snippets, pages, API routes, and the plugin manifest, pushing each change to the `local` environment automatically. Supports `--only`/`--skip` to limit which resource types are watched.
