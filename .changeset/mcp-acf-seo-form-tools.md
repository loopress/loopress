---
"@loopress/mcp": minor
---

The MCP server now exposes `acf`, `seo` and `form` tools (`_push`, `_pull`, `_list`), matching the CLI. `acf_*` takes an optional `type` array and `seo_*` an optional `postType` array to scope the operation.
