---
title: Getting Started
description: Install the Loopress plugin and CLI and sync your first snippet in about 3 minutes.
---

import PluginDownload from '../../components/PluginDownload.astro';

Loopress has two parts: a WordPress plugin and a CLI.

The plugin works standalone, straight from the WordPress admin, for Composer dependency management. The CLI requires the plugin to be installed and active on the site you want to manage.

## Prerequisites

- A WordPress installation (6.0+) with administrator access
- PHP 8.2+ on the server
- Node.js 18+ on your machine, for the CLI

## Quick start (about 3 minutes)

### 1. Install the Loopress plugin

<PluginDownload edition="full" />

Upload `loopress-full.zip` via **Plugins → Add New → Upload Plugin**, then activate it. See the [plugin page](/wordpress-plugin/) for details. Loopress Full is the recommended default: it includes everything below plus Composer dependency management, at no extra cost. If you specifically want the lighter edition without Composer support, install [Loopress Light](/wordpress-plugin/) (`loopress-light.zip`) instead.

This step is required before the CLI can talk to the site.

### 2. Install the CLI

```bash
npm install -g @loopress/cli
```

### 3. Connect it to your site

```bash
lps project config
```

Enter your WordPress URL, then choose **Authorize in my browser (recommended)**. This opens your WordPress admin and creates an Application Password for you automatically. If it can't complete (or you'd rather enter credentials yourself), fall back to manual entry, see the [Application Passwords guide](/cli/application-passwords/) if you can't find that section in your WordPress admin.

### 4. Sync your first snippets

```bash
# Download your existing snippets as .php files
lps snippet pull

# Edit locally, commit to Git
git add snippets/ && git commit -m "chore: import snippets"

# Push changes back to WordPress
lps snippet push
```

This works with either [WPCode](https://wpcode.com/) or the [Code Snippets](https://wordpress.org/plugins/code-snippets/) plugin, whichever is active on the site.

---

Need Composer or plugin version management too? Continue with the [CLI reference](/cli/) or the [WordPress plugin docs](/wordpress-plugin/).

Want to see it all wired together, including CI and E2E tests? [github.com/loopress/demo](https://github.com/loopress/demo) is a small reference project you can clone.
