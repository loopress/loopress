{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Loopress composer audit (REST)",
  "description": "GET /loopress/v1/composer/audit response shape (ComposerService::audit(), passed through from `composer audit --format=json`).",
  "type": "object",
  "additionalProperties": false,
  "required": ["advisories", "abandoned"],
  "properties": {
    "advisories": { "type": ["object", "array"] },
    "abandoned": { "type": ["object", "array"] }
  }
}
