{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Loopress composer operation result (REST)",
  "description": "POST /loopress/v1/composer/{require,remove,repair,sync} success response shape.",
  "type": "object",
  "additionalProperties": false,
  "required": ["message", "output"],
  "properties": {
    "message": { "type": "string" },
    "output": { "type": "string" }
  }
}
